1. Fixed the latency issue when using with Switch Joy-cons attached. 
2. Add the HOT KEY for SNES/NES Classic controller: Press and hold HOT KEY for 5 seconds to map DPAD and other buttons, LED will blink once in red when successful: SELECT + UP: DPAD = DPAD SELECT + LEFT: DPAD = LEFT JOYSTICK SELECT + RIGHT: DPAD = RIGHT JOYSTICK
